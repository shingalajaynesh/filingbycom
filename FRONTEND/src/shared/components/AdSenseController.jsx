/**
 * Google-served ads are intentionally disabled during AdSense review.
 * This controller is kept as a no-op compatibility shim for older imports.
 */
export default function AdSenseController() {
  return null;
}
